document.addEventListener('DOMContentLoaded', () => {
        // اسکریپت برای اسکرول به بالای صفحه هنگام ریلود
        window.onload = function() {
            window.scrollTo(0, 0);
        };
    });