"""
Tests for the notifications app, mirroring the split in `views/`.

`test_sms.py` covers number parsing, batching and the spend guards.
`test_push.py` covers subscriptions, delivery, and the manifest/service-worker
files that make the site installable.
"""
