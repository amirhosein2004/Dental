"""
Tests for the core app.

One module for now: repo-wide hygiene checks. These are not tests of the core
app's own views — they live here because core is the app with no public
surface of its own, which makes it the natural home for checks that span every
template and the settings module.
"""
