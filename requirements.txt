# =============================================================================
# Dental Clinic Management System — direct runtime dependencies only.
# Transitive packages (kombu, billiard, botocore, cryptography, Pillow deps,
# django-ckeditor's Willow/draftjs_exporter/telepath, etc.) are installed
# automatically by pip; do NOT add them here manually.
# =============================================================================

# --- Core framework --------------------------------------------------------
# 5.2 is the LTS line, supported to April 2028. 5.1 reached end of life and
# stopped receiving security releases, which is the reason for the move —
# not any single advisory.
Django==5.2.17

# --- Data layer ------------------------------------------------------------
psycopg2-binary==2.9.10       # PostgreSQL driver (prod)
redis==5.2.1                  # Cache backend + Celery broker

# --- Background tasks ------------------------------------------------------
celery==5.4.0

# --- Django third-party apps ----------------------------------------------
django-ckeditor-5==0.2.17     # Rich text editor for blog content
django-filter==24.3           # Query filtering for list views
django-ratelimit==4.1.0       # Per-view rate limiting mixin
django-storages==1.14.6       # S3-compatible media storage (Liara bucket)
nh3==0.2.20                   # HTML sanitiser for blog bodies rendered with |safe
pywebpush==2.0.3              # Web Push (browser notifications) for staff alerts
Pillow==12.3.0                # Image validation + captcha rendering

# --- Cloud storage ---------------------------------------------------------
boto3==1.37.27                # S3 client used by django-storages

# --- Persian/Jalali dates --------------------------------------------------
jdatetime==5.1.0

# --- Environment / secrets -------------------------------------------------
python-dotenv==1.2.3

# --- Production ------------------------------------------------------------
gunicorn==23.0.0              # WSGI server

# --- Development -----------------------------------------------------------
django-debug-toolbar==6.0.0
